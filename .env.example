# Required -- connection string for the ice-maker staging database
ICEMAKER_DB_URL=postgresql://user:pass@localhost:5432/icemaker

# Optional -- public API used for UUID alignment during promotion (default shown)
# SKATETRAX_API_URL=https://api.skatetrax.com/api/v4/public/rinks

# Optional -- only needed for --sync-ice-time (confirms rinks via Skatetrax sessions)
SKATETRAX_DB_URL=postgresql://user:pass@localhost:5432/skatetrax
